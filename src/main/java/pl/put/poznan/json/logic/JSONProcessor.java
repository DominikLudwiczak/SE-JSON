package pl.put.poznan.json.logic;

class JSONProcessor implements JSONTools {
    @Override
    public String processJSON(String jsonData) {
        // Perform any initial processing on the JSON data
        return jsonData;
    }
}